import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appAlertBlank',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
